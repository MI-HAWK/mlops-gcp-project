[pytest]
testpaths = tests
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (may need fixtures)
    sanity: Smoke/sanity tests (quick health checks)
addopts = -v --tb=short

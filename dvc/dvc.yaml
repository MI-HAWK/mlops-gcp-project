stages:
  train:
    cmd: python src/model/train.py
    deps:
      - src/model/train.py
      - data/
    metrics:
      - metrics.txt:
          cache: false
    outs:
      - models/encoders.joblib

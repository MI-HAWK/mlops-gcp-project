apiVersion: v1
kind: Service
metadata:
  name: ml-api-staging-svc
  namespace: default
spec:
  selector:
    app: ml-api-staging
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
